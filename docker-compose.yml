version: '3.4'
services:
  redis:
    image: "redis:alpine"
    command: redis-server
    ports:
      - "6379:6379"
    environment:
      - REDIS_REPLICATION_MODE=master

  mongodb:
    image: mongo:latest
    environment:
      - MONGODB_DATABASE="paymaster"
    ports:
      - 27017:27017

